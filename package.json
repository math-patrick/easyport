{
  "name": "nozmie-addon",
  "version": "2.2.0-alpha",
  "description": "Nozmie WoW Addon packaging automation",
  "scripts": {
    "bump:alpha": "npm version prepatch --preid=alpha && git push && git push --tags",
    "tag:alpha": "git tag v$(node -p 'require(\"./package.json\").version') && git push origin v$(node -p 'require(\"./package.json\").version')"
  },
  "devDependencies": {}
}
