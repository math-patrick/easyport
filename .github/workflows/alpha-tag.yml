name: Alpha Auto Tag

on:
  push:
    branches:
      - '**'

permissions:
  contents: write

jobs:
  tag-alpha:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Set Git credentials
        run: |
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

      - name: Bump alpha version
        run: |
          npm version patch --preid=alpha --no-git-tag-version
          git add package.json
          git commit -m "chore: bump alpha version [skip ci]" || echo "No changes to commit"
          git push

      - name: Tag and push tag (main only)
        if: github.ref == 'refs/heads/main'
        run: |
          git tag v$(node -p "require('./package.json').version")
          git push --tags

      - name: Create Pull Request
        if: github.ref != 'refs/heads/main'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: bump alpha version [skip ci]"
          title: "Alpha version bump"
          body: "Automated alpha version bump and PR creation."
          branch: ${{ github.ref_name }}
          base: main
